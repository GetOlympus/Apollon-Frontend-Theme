<?php

/**
 * Core options
 *
 * @package    ApollonFrontendTheme
 * @author     Achraf Chouk <achrafchouk@gmail.com>
 * @since      0.0.1
 */

return [
// CORE
    /*$slug.'-core' => array_merge($this->contents['section_title'], [
        'title'    => __('apollon.cz.components.core.title', OL_APOLLON_DICTIONARY),
        'priority' => ++$priority,
    ]),

    // Article
    $slug.'-article' => [
        'title'    => 'Article',
        'priority' => ++$priority,
        'controls' => [],
    ],

    // Background
    $slug.'-background' => [
        'title'    => 'Background',
        'priority' => ++$priority,
        'controls' => [],
    ],

    // Base
    $slug.'-base' => [
        'title'    => 'Base',
        'priority' => ++$priority,
        'controls' => [],
    ],

    // Breadcrumb
    $slug.'-breadcrumb' => [
        'title'    => 'Breadcrumb',
        'priority' => ++$priority,
        'controls' => [],
    ],

    // Button
    $slug.'-button' => [
        'title'    => 'Button',
        'priority' => ++$priority,
        'controls' => [],
    ],

    // Comment
    $slug.'-comment' => [
        'title'    => 'Comment',
        'priority' => ++$priority,
        'controls' => [],
    ],

    // Form
    $slug.'-form' => [
        'title'    => 'Form',
        'priority' => ++$priority,
        'controls' => [],
    ],

    // Heading
    $slug.'-heading' => [
        'title'    => 'Heading',
        'priority' => ++$priority,
        'controls' => [],
    ],

    // Link
    $slug.'-link' => [
        'title'    => 'Link',
        'priority' => ++$priority,
        'controls' => [],
    ],

    // Pagination
    $slug.'-pagination' => [
        'title'    => 'Pagination',
        'priority' => ++$priority,
        'controls' => [],
    ],

    // Search
    $slug.'-search' => [
        'title'    => 'Search',
        'priority' => ++$priority,
        'controls' => [],
    ],

    // Text
    $slug.'-text' => [
        'title'    => 'Text',
        'priority' => ++$priority,
        'controls' => [],
    ],

    // Utility
    $slug.'-utility' => [
        'title'    => 'Utility',
        'priority' => ++$priority,
        'controls' => [],
    ],*/
];
